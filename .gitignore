node_modules/
.dev.vars
.wrangler/
# 本地部署配置：含真实 D1 database_id，不进仓库（用 wrangler.toml 复制后填 ID）
worker/wrangler.local.toml
# 构建产物：由 npm run build:ui 从 frontend/ 生成，不进仓库
worker/src/ui.js
npm-debug.log*
*.log
.DS_Store
Thumbs.db
# fetch 顺手留下的原始下载档（17500 全量 txt，约 4MB）不进仓库；
# 解析后的 scripts/randomness/data/*.json 才提交 —— 文档里的统计结论要能离线复现。
scripts/randomness/data/*_asc.txt
# 最近一次 fetch 的交叉校验状态（临时产物，结论已印进 docs/randomness-latest.md 第 0 节）
scripts/randomness/data/crosscheck.json
# 内部工作记录：进度日志与跨会话续接文档。它们是给自己用的草稿，会写到本机路径、代理端口、
# 自己的站点地址，因此不进公开仓库；想留档就在本地放着，或者写到仓库外的目录。
progress.md
docs/continue-*.md
